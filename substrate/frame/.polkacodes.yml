scripts:
  check: cargo test -p pallet-honzon
rules: |
  Use traits/tokens/fungible(s) traits
  Use pallet-assets in mock for testing
  Use T::BlockNumberProvider::current_block_number() to get current block number
